# Быстрое исправление авторизации в продакшене

## Что было исправлено

1. ✅ **Cookies теперь работают в HTTPS** (`secure: true` для продакшена)
2. ✅ **Cross-site cookies** (`sameSite: 'none'` для продакшена)
3. ✅ **CORS настроен через переменную окружения**

## Что нужно сделать СЕЙЧАС

### 1. Установите переменную окружения на продакшене:

```bash
CORS_ORIGIN=https://your-frontend-domain.com
```

**Важно:** Замените `https://your-frontend-domain.com` на реальный домен вашего фронтенда!

### 2. Убедитесь, что установлено:

```bash
NODE_ENV=production
```

### 3. Перезапустите приложение

После установки переменных окружения перезапустите приложение.

## Проверка

После перезапуска проверьте логи - должно быть:

```
🌐 CORS configuration: {
  isProduction: true,
  corsOrigin: 'https://your-frontend-domain.com',
  origin: ['https://your-frontend-domain.com']
}
```

## Если не работает

1. Проверьте, что домен в `CORS_ORIGIN` точно совпадает с доменом фронтенда
2. Проверьте, что фронтенд отправляет запросы с `withCredentials: true` (уже настроено)
3. Проверьте логи бэкенда на наличие ошибок
4. Проверьте Network в DevTools - запрос должен доходить до сервера

## Пример для CapRover

В CapRover Dashboard → App Settings → Environment Variables:

```
NODE_ENV=production
CORS_ORIGIN=https://eternum-book.netlify.app
```

(Замените на ваш реальный домен)

