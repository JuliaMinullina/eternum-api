# Исправление проблемы авторизации в продакшене

## Проблема
После деплоя на продакшен авторизация не работает:
- Ввод логина и пароля → страница обновляется → снова на странице авторизации
- Запрос login в devtools загорается красным и сразу пропадает

## Причина
Проблема связана с настройками cookies и CORS для продакшена:
1. **Cookies с `secure: false`** не работают в HTTPS (продакшен)
2. **`sameSite: 'lax'`** может блокировать cross-site cookies
3. **CORS** может быть неправильно настроен для продакшена

## Исправления

### 1. Обновлены настройки cookies (`auth-cookies.middleware.ts`)
- ✅ `secure: true` для продакшена (HTTPS)
- ✅ `sameSite: 'none'` для продакшена (cross-site cookies)
- ✅ Автоматическое определение окружения через `NODE_ENV`

### 2. Улучшены настройки CORS (`main.ts`)
- ✅ Поддержка переменной окружения `CORS_ORIGIN` для продакшена
- ✅ Логирование конфигурации CORS при старте
- ✅ Автоматическое разделение нескольких доменов через запятую

## Действия для исправления на продакшене

### 1. Убедитесь, что переменные окружения установлены:

```bash
NODE_ENV=production
CORS_ORIGIN=https://your-frontend-domain.com
```

**Важно:** 
- Укажите точный домен фронтенда (без слэша в конце)
- Если несколько доменов, разделите запятой: `https://domain1.com,https://domain2.com`
- Домен должен совпадать с тем, откуда отправляются запросы

### 2. Перезапустите приложение

После установки переменных окружения перезапустите приложение, чтобы изменения вступили в силу.

### 3. Проверьте логи при старте

В логах должно появиться:
```
🌐 CORS configuration: {
  isProduction: true,
  corsOrigin: 'https://your-frontend-domain.com',
  origin: ['https://your-frontend-domain.com']
}
```

### 4. Проверьте настройки cookies в браузере

После успешного логина проверьте в DevTools → Application → Cookies:
- `access_token` должен иметь флаги: `HttpOnly`, `Secure`, `SameSite=None`
- `refresh_token` должен иметь флаги: `HttpOnly`, `Secure`, `SameSite=None`

## Дополнительная диагностика

### Если проблема сохраняется:

1. **Проверьте CORS preflight запросы:**
   - В DevTools → Network найдите запрос `OPTIONS /auth/login`
   - Проверьте заголовки ответа:
     - `Access-Control-Allow-Origin` должен содержать ваш домен
     - `Access-Control-Allow-Credentials: true`
     - `Access-Control-Allow-Methods` должен содержать `POST`

2. **Проверьте логи бэкенда:**
   - Ищите сообщения `🔐 Login attempt for:`
   - Ищите сообщения `🍪 Cookies set successfully`
   - Проверьте наличие ошибок

3. **Проверьте переменные окружения:**
   ```bash
   # В CapRover или на сервере
   echo $NODE_ENV
   echo $CORS_ORIGIN
   ```

4. **Проверьте, что фронтенд отправляет запросы с credentials:**
   - В `httpAdapter.ts` должно быть `withCredentials: true`
   - В запросах должен быть заголовок `Content-Type: application/json`

## Важные замечания

### Для SameSite=None требуется Secure
Если используете `sameSite: 'none'`, cookies **обязательно** должны иметь флаг `secure: true`. Браузеры блокируют cookies с `SameSite=None` без `Secure` флага.

### CORS и credentials
При использовании `credentials: true` в CORS, нельзя использовать `origin: '*'`. Нужно явно указать домены в `CORS_ORIGIN`.

### Проверка в браузере
Некоторые браузеры могут блокировать cookies с `SameSite=None` в определенных контекстах. Проверьте в разных браузерах.

## Откат изменений (если нужно)

Если нужно временно откатить изменения:
1. Установите `NODE_ENV=development` (но это не рекомендуется для продакшена)
2. Или временно измените `secure: false` и `sameSite: 'lax'` в коде

## Контакты для поддержки

Если проблема не решается, проверьте:
- Логи приложения на сервере
- Логи браузера (Console и Network в DevTools)
- Настройки прокси/балансировщика (если используется)

